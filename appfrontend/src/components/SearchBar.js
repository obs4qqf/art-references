const SearchBar = () => {
    return (
        <div id="search">
            <h1>Enter a keyword to search:</h1>
            <input type="text" />
            <button>Search</button>
        </div>
    )
}

export default SearchBar
